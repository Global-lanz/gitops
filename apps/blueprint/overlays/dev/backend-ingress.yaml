apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: blueprint-backend
spec:
  ingressClassName: traefik
  rules:
    - host: api.blueprint-dev.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: blueprint-backend
                port:
                  number: 3333