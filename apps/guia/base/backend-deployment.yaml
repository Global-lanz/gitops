apiVersion: apps/v1
kind: Deployment
metadata:
  name: guia-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: guia-backend
  template:
    metadata:
      labels:
        app: guia-backend
    spec:
      containers:
        - name: guia-backend
          image: maiconlanzendorf/n8n-chat-backend:1.4.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: guia-config
            - secretRef:
                name: guia-secret
          resources:
            requests:
              cpu: 25m
              memory: 96Mi
            limits:
              cpu: 250m
              memory: 320Mi
          readinessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 20
