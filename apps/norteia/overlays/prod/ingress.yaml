apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: norteia-frontend
spec:
  ingressClassName: traefik
  rules:
    - host: norteia.ionflux.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: norteia-frontend
                port:
                  number: 80
