apiVersion: apps/v1
kind: Deployment
metadata:
  name: n8n-chat-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: n8n-chat-frontend
  template:
    metadata:
      labels:
        app: n8n-chat-frontend
    spec:
      containers:
        - name: n8n-chat-frontend
          image: maiconlanzendorf/n8n-chat-frontend:1.4.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          # The Angular app reads API_BASE_URL / AUTH_PORTAL_URL at container
          # start (docker-entrypoint -> env-config.js). Pulled from the shared
          # config; backend-only keys are ignored by nginx.
          envFrom:
            - configMapRef:
                name: chat-n8n-config
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 20
